name: Update READMEs

on:
  push:
    paths:
      - '**.md'
      - 'tools/**'
      - '.github/workflows/update-readme.yml'
      - '01-*/**'
      - '02-*/**'
      - '03-*/**'
      - '04-*/**'
      - '05-*/**'
      - '06-*/**'
      - '07-*/**'
      - '08-*/**'
      - '09-*/**'
      - '10-*/**'
  workflow_dispatch: {}

jobs:
  update-readmes:
    name: Generate and Commit Updated READMEs
    runs-on: ubuntu-latest
    permissions:
      contents: write   # ✅ allow write access with GITHUB_TOKEN

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run README update script
        run: |
          python3 tools/update_readmes.py

      - name: Commit README changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md */README.md || true
          if git diff --cached --quiet; then
            echo "No README changes to commit."
            exit 0
          fi
          git commit -m "chore(docs): regenerate READMEs (automated)"
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }} HEAD:${{ github.ref }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
